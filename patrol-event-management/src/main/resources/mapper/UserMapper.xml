<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
   mapper标签:配置各类声明
   namespace：名称空间，由于映射文件可能有多个，为了防止crud语句的唯一标识被重复，需要设置空间名称。
 -->

<mapper namespace="com.icbc.patrol.mapper.UserMapper">
    <!--
        select:查询的statement（声明），用来编写查询语句
        id:语句的唯一标识
        resultType：配置返回的结果集类型
        parameterType：传递的参数类型，可以省略
    -->

    <resultMap id="UserResultMap" type="com.icbc.patrol.model.User">
        <id column="id" jdbcType="BIGINT" property="id"></id>
        <result column="name" jdbcType="VARCHAR" property="name"></result>
        <result column="age" jdbcType="INTEGER" property="age"></result>
    </resultMap>

    <select id="queryUserById" parameterType="java.lang.Long" resultMap="UserResultMap">
        select id, name, age from tb_user where id = #{id,jdbcType=BIGINT}
    </select>

    <!--    <select id="queryUserList" resultMap="UserResultMap" parameterType="com.icbc.patrol.model.User">-->
    <!--        select id, name, age from tb_user-->
    <!--    </select>-->

    <insert id="addUser" parameterType="com.icbc.patrol.model.User">
        insert into
        tb_user(id,
                name,
                age)
        values (#{id,jdbcType=BIGINT},
                #{name,jdbcType=VARCHAR},
                #{age,jdbcType=INTEGER})
    </insert>

    <delete id="deleteUserById" parameterType="java.lang.Long">
        delete from tb_user where id = #{id,jdbcType=BIGINT}
    </delete>

    <update id="updateUserById" parameterType="com.icbc.patrol.model.User">
        update tb_user
        set name = #{name,jdbcType=VARCHAR}, age=#{age,jdbcType=INTEGER}
        where id = #{id,jdbcType=INTEGER}
    </update>

</mapper>
